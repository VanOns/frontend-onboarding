import Icon from "./components/Icon";

const App = () => {
  return (
    <div className={"flex items-center justify-center min-h-screen"}>
      <Icon className={"w-24 h-24 animate-bounce"} icon={"cow"} />
    </div>
  );
};

export default App;
